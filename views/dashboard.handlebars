<div id="button-display">
    <button onclick="newFormHandler()"></button>
</div>

{{#each userInfo }}
<div class="home-blog">
  <div class="home-row ">
      <a class="home-text">{{title}}</a>
    <p class="home-text">{{format_date date_created}}</p>
  <button onclick="updateFormDisplayHandler(event)" data-updateId="{{id}}" class="login-button">Update</button>
  <button onclick="delButtonHandler(event)" data-deleteId="{{id}}" class="login-button">Delete</button>
  </div>
</div>
<div id="updateForm{{id}}"></div>
{{/each}}

<script src="/js/post.js"></script>